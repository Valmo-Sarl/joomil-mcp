# joomil-mcp

MCP server for [Joomil.ch](https://www.joomil.ch) — Swiss classifieds marketplace.

Exposes 3 tools: `search_classifieds`, `get_classified`, `get_categories`.

Built with Cloudflare Workers + MCP SDK.

## Deploy

```bash
npm install
npm run deploy
```
